const products = []
const product = products[0]

// Truthy Values that resollve to true in boolean context
// Falsy Values that resolve to false in boolean context
// Falsy => false, 0, empty string, null, undefined

if (product) {
    console.log("Present")
} else {
    console.log("Not Present")
}